#pragma once

// Copy this file to `secrets.h` and fill in your WiFi credentials.
// `secrets.h` is gitignored so your password never gets committed.

#define WIFI_SSID     "YourNetworkName"
#define WIFI_PASSWORD "YourNetworkPassword"
